import "./Hero.scss";

const Hero = () => {
  return (
    <section className={"hero"}>
      <h1 className={"heading-primary"}>
        <span className={"heading-primary-main"}>Gilberto</span>

        <span className={"heading-primary-sub"}>Desenvolvedor Front-end</span>
      </h1>
    </section>
  );
};

export default Hero;
